import type { Metadata } from "next";
import PageShell from "@/components/marketing/PageShell";
import { requireSession } from "@/lib/auth";
export const metadata: Metadata = { title: "Payroll & Time Card - Bare Studios OS" };
const payroll = [["Ciara", "72.5 hrs", "$8,420 revenue", "70/30 commission", "$5,894 est."], ["Na", "18 hrs", "$2,260 revenue", "70/30 commission", "$1,582 est."], ["Front desk", "40 hrs", "$18/hr", "Hourly", "$720 est."]];
export default async function PayrollPage(){await requireSession();return <PageShell eyebrow="Reports" title="Payroll, time card, and rent collection." intro="Track hours, time off, service revenue, commission structures, payroll history, and rent collection in one payroll workbench." wide><div className="grid gap-3 sm:grid-cols-4"><div className="rounded-xl border border-border bg-surface p-5"><p className="font-serif text-3xl">130.5</p><p className="text-xs uppercase tracking-[0.14em] text-text-muted">hours</p></div><div className="rounded-xl border border-border bg-surface p-5"><p className="font-serif text-3xl">$10.7k</p><p className="text-xs uppercase tracking-[0.14em] text-text-muted">payroll est.</p></div><div className="rounded-xl border border-border bg-surface p-5"><p className="font-serif text-3xl">3</p><p className="text-xs uppercase tracking-[0.14em] text-text-muted">time off requests</p></div><div className="rounded-xl border border-border bg-surface p-5"><p className="font-serif text-3xl">$2.4k</p><p className="text-xs uppercase tracking-[0.14em] text-text-muted">rent due</p></div></div><section className="mt-8 rounded-xl border border-border bg-surface p-6"><h2 className="font-serif text-2xl">Payroll preview</h2><div className="mt-4 overflow-hidden rounded-lg border border-border">{payroll.map((r,i)=><div key={r[0]} className={`grid gap-2 bg-white p-4 text-sm md:grid-cols-5 ${i>0?'border-t border-border':''}`}>{r.map(c=><span key={c}>{c}</span>)}</div>)}</div></section></PageShell>}
