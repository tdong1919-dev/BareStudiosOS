import type { Metadata } from "next";
import PageShell from "@/components/marketing/PageShell";
import { requireSession } from "@/lib/auth";
export const metadata: Metadata = { title: "Import & Cleanup - Bare Studios OS" };
const dupes = [["Jasmine Rivera", "2 possible duplicates", "Same phone"], ["Sandra M.", "3 possible duplicates", "Same email"], ["Maya Lee", "2 possible duplicates", "Similar name"]];
export default async function ImportsPage(){await requireSession();return <PageShell eyebrow="Customers" title="Import management and customer cleanup." intro="Batch import CSVs, review field mapping, merge duplicates, clean incomplete records, and invite all customers into the new experience." wide><div className="flex flex-wrap gap-3"><label className="cursor-pointer rounded-sm bg-gradient-brand px-5 py-3 text-[12px] uppercase tracking-[0.14em] text-white">Batch import CSV<input type="file" accept=".csv" className="sr-only" /></label><button className="rounded-sm border border-border px-5 py-3 text-[12px] uppercase tracking-[0.14em]">Invite all customers</button><button className="rounded-sm border border-border px-5 py-3 text-[12px] uppercase tracking-[0.14em]">Share with another account</button></div><section className="mt-8 rounded-xl border border-border bg-surface p-6"><h2 className="font-serif text-2xl">Merge & clean up customers</h2><div className="mt-4 overflow-hidden rounded-lg border border-border">{dupes.map(([name,count,reason],i)=><div key={name} className={`grid gap-2 bg-white p-4 text-sm sm:grid-cols-4 ${i>0?'border-t border-border':''}`}><strong>{name}</strong><span>{count}</span><span>{reason}</span><button className="text-left text-text-primary underline">Review merge</button></div>)}</div></section></PageShell>}
