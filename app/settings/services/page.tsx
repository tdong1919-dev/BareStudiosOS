import type { Metadata } from "next";
import PageShell from "@/components/marketing/PageShell";
import { requireSession } from "@/lib/auth";
export const metadata: Metadata = { title: "Service Menu & Availability - Bare Studios OS" };
const services = [["Classic lash extensions", "Na, Ciara", "$150+", "120 min"], ["Custom facial", "Ciara", "$95+", "60 min"], ["Barbering", "Andy", "Call/text only", "Varies"], ["Women's hair", "Cindy", "Call/text only", "Varies"]];
export default async function ServicesPage(){await requireSession();return <PageShell eyebrow="Settings" title="Service menu and availability." intro="Configure services, providers, pricing, durations, booking rules, available hours, and time-off blocks." wide><section className="rounded-xl border border-border bg-surface p-6"><h2 className="font-serif text-2xl">Service / class menu</h2><div className="mt-4 overflow-hidden rounded-lg border border-border">{services.map(([name,providers,price,duration],i)=><div key={name} className={`grid gap-2 bg-white p-4 text-sm md:grid-cols-5 ${i>0?'border-t border-border':''}`}><strong>{name}</strong><span>{providers}</span><span>{price}</span><span>{duration}</span><button className="text-left underline">Edit</button></div>)}</div></section><section className="mt-8 rounded-xl border border-border bg-surface p-6"><h2 className="font-serif text-2xl">Provider availability</h2><div className="mt-4 grid gap-3 md:grid-cols-4">{["Ciara: through Aug 1", "Na: Sundays 9-7", "Andy: call/text", "Cindy: call/text"].map(x=><input key={x} className="rounded-md border border-border bg-white px-3 py-2 text-sm" defaultValue={x} />)}</div></section></PageShell>}
